import { createContext } from "react";

export const CounterContext = createContext(null);

//Step 1
//Humne subse pehle ek "context" naam ka folder banaya which stores all the contexts and then we created as "CounterContext file" in which humne "createContext" library ko import kiya react se and then Context create kiya and create karke ek variable me save kar diya and then exported it .